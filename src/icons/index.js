import Icon from '@ant-design/icons'
import History from 'src/icons/icons/history.js'
import BackArrow from 'src/icons/icons/backArrow.js'

export const HistoryIcon = (props) => <Icon component={History} {...props} />

export const BackArrowIcon = (props) => <Icon component={BackArrow} {...props} />
